{
  "name": "csf-live",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun --cwd packages/web run dev",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "bun --cwd packages/web run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8"
  }
}
